---
id: 2
title: Review control path coverage
epic: null
state: IMPLEMENTING
risk_proposed: MEDIUM
risk_reason: kontrol mekanizmasının kapsamını değiştiriyor
complexity_proposed: LOW
modules:
- authority
blocked_by: []
adr: []
floor_risk: HIGH
floor_matched:
- 'devflow/** (from module: authority)'
- devflow/** (from issue)
- '**/*.md (from issue)'
signals: null
architecture_impact: null
uncertain: false
floor_risk_actual: null
floor_matched_actual: []
blocked_from: null
blocked_reason: null
review_records: []
---
# Task 2 — Review control path coverage

## Issue

CONTROL_PATHS listesi dört kez elle genişletildi ve her seferinde bir CI
hatasıyla keşfedildi. Liste baştan taranarak eksikler bulunmalı.

Repo kökündeki dosyalar tek tek değerlendirilecek: hangileri denetim
mekanizmasının parçası? pyproject.toml bağımlılık sürümlerini tanımlıyor
ve verify'ın davranışını değiştirebilir — muhtemelen listede olmalı.

## Acceptance

- CONTROL_PATHS'e eklenen her yol için gerekçe yorum satırı olmalı
- Mevcut testler bozulmamalı
- Eklenen her yol için en az bir test
## Plan

## Plan â€” Task 2: Review control path coverage

*Plan only. No code changed, so `./scripts/verify` was not run.*

### Flags before implementation

- **Risk floor mismatch.** `.devflow/tasks/1.md` carries `risk_proposed: MEDIUM`, but the change lands in `devflow/**` + `tests/**`, and `.ai/policy.yml:7` puts `devflow/**` at **HIGH** floor â†’ `plan_approval: true`. Treat this as HIGH: this plan needs human approval before apply.
- **Task file id.** The prompt says Task 2 and the branch is `task/2-â€¦`, but only `.devflow/tasks/1.md` exists (same title/body). Confirm which id to append notes to.

### Sweep result â€” repo root, file by file

Already covered: `.ai/**`, `.githooks/**`, `.gitignore`, `devflow/**`, `templates/project/**`, `scripts/verify` + `scripts/verify.d/**`, `.github/workflows/**`.

Gaps found (4):

- **`pyproject.toml` â€” add.** Holds `[tool.ruff.lint] select`, `[tool.mypy] strict`/`files`, `[tool.pytest.ini_options] testpaths`, plus the dep pins. All four `verify.d` stages read it. Flipping `strict = false` or trimming `select` weakens verify without touching `scripts/**` â€” the exact bypass CONTROL_PATHS exists to stop.
- **`scripts/setup-hooks` â€” uncovered.** It installs `core.hooksPath=.githooks`; editing it disables pre-commit/pre-push. Fix by collapsing `scripts/verify` + `scripts/verify.d/**` into **`scripts/**`** â€” no non-control script exists in this repo, and enumerating stage files individually is what caused the repeated manual widening.
- **`.github/` beyond workflows â€” uncovered.** `.ai/policy.yml:7` already floors `.github/**` at HIGH, but CONTROL_PATHS only matches `.github/workflows/**`. CODEOWNERS, `dependabot.yml`, PR templates, composite actions all gate review. Broaden to **`.github/**`** so the two lists agree.
- **`templates/.gitkeep` â€” uncovered.** Sits outside `templates/project/**`; keeps the dir present for `devflow init`. Broaden to **`templates/**`** (one rule, no non-control content under `templates/`).

Considered and deliberately left out: `tests/**`, `README.md`, `.devflow/tasks/**` stay in `_ALLOWED_WITH_CONTROL` â€” promoting them would fail every app-code PR that adds a test. No lockfile is tracked today; if `uv.lock`/`requirements*.txt` appears later it belongs on the list.

### Changes

- `devflow/authority.py`: four pattern edits above, each with a one-line rationale comment (acceptance criterion 1).
- `tests/test_authority.py`: **`[REDACTED]` (line 259) will fail** â€” it asserts the literal strings `".github/workflows/**"` and membership by exact pattern. Rewrite it to assert *behavior* (`check_control_changes([".github/CODEOWNERS"]).touches_control`) rather than list literals, so the next widening doesn't churn it. This is an intentional update, not a regression; all other existing tests pass unchanged (`test_verify_script_with_docs_ok` still matches under `scripts/**`).
- New tests, â‰¥1 per added path (acceptance criterion 3): `pyproject.toml` alone is control / ok with `devflow/**` / **not** ok with `src/**` / `is_control_change` true; `scripts/setup-hooks` alone is control; `.github/CODEOWNERS` alone is control; `templates/.gitkeep` alone is control.
- Add one **coverage-drift guard**: walk `git ls-files` root entries and assert each is classified as control or an explicit allowed-companion. This is the fix for the actual issue â€” five manual widenings, each found by a CI failure. Slightly beyond "add the missing paths"; say the word if you want it dropped.

### Second-order effect worth an explicit decision

Adding `pyproject.toml` also tightens `check_control_changes` and widens `is_control_change`: a PR that bumps a dependency **and** touches `src/**` will now FAIL and demand a split, and such PRs lose the fast lane (`devflow/ci_checks.py:45`). That is the intent, but it changes day-to-day PR shape in consumer projects â€” confirm before apply.

### Doc impact

`none` â€” no user-facing behavior or documented contract changes; the rationale lives in code comments per acceptance criterion 1.
## Transition

BACKLOG → PLAN_APPROVAL (start)
## Transition

PLAN_APPROVAL → IMPLEMENTING (plan_approved)

## Implementation notes

Applied as planned. Two files changed: `devflow/authority.py`, `tests/test_authority.py`.

### CONTROL_PATHS

Now: `.ai/**`, `scripts/**`, `.github/**`, `.githooks/**`, `devflow/**`,
`templates/**`, `.gitignore`, `pyproject.toml`. Each added or broadened
pattern carries a rationale comment (acceptance criterion 1).

- `scripts/verify` + `scripts/verify.d/**` → `scripts/**`: covers
  `scripts/setup-hooks`, which installs `core.hooksPath=.githooks`.
- `.github/workflows/**` → `.github/**`: CODEOWNERS, dependabot config, PR
  templates, composite actions. Now agrees with `.ai/policy.yml`, which
  already floors all of `.github/**` at HIGH.
- `templates/project/**` → `templates/**`: covers `templates/.gitkeep`.
- `pyproject.toml` added: configures every verify stage (ruff select, mypy
  strict/files, pytest testpaths) plus the tool pins.

### Tests

- Rewrote `test_control_paths_include_github_workflows` (asserted pattern
  string literals, so any broadening broke it) as
  `test_control_paths_cover_known_control_files`, which asserts behaviour on
  representative files. Intentional, and the only existing test touched.
- New tests, ≥1 per added path: `pyproject.toml` (alone / with `devflow/**` /
  with `src/**` / `is_control_change`), `scripts/setup-hooks` (2),
  `.github/CODEOWNERS`, `templates/.gitkeep`.
- New `test_every_tracked_file_is_classified`: walks `git ls-files` and fails
  if any tracked file matches neither `CONTROL_PATHS` nor
  `_ALLOWED_WITH_CONTROL`. This is the guard against a fifth manual widening
  discovered by a CI failure on an unrelated PR.

`./scripts/verify` passes: 316 passed, 1 skipped.

### Environment note (not a code change)

Running verify inside this worktree needs a worktree-local `.venv`. The main
checkout's venv has an editable-install `.pth` pointing at the main repo, and
under pytest's prepend import mode that path wins over the worktree — so
tests silently exercise the main checkout's `devflow` package. Created
`.venv/` here via `pip install -e ".[dev]"`; it is gitignored.

### Known effect of adding pyproject.toml

A PR that bumps a dependency **and** touches `src/**` now fails
`check_control_changes` and must be split, and loses the fast lane
(`devflow/ci_checks.py:45`). Intended, per the approved plan.

### Doc impact

Status: `none` — no user-facing behaviour or documented contract changed; the
rationale lives in code comments per acceptance criterion 1.
## Verify

/bin/bash: C:[REDACTED].devflowworktreesta[REDACTED]: No such file or directory
## Transition

IMPLEMENTING → IMPLEMENTING (verify_failed)
